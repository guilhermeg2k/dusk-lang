let mut i = 0

for i < 5000000
    let trash = [1, 2, 3, i]
    let trash_struct = @(x = i, y = i * 2)
    i += 1

assert(i==5000000)
echo("Survive")

let arr = [1]
let survival = @(segredo = 42, nome = "Dusk", arr = arr)
let arr2 = [survival]

i = 0
for i < 1000000
    let trash = @(id = i)
    i += 1

echo("Survive")
